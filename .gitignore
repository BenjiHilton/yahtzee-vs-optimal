__pycache__/
*.pyc
.DS_Store
# local Claude Code config (not part of the app)
.claude/
# local run artifacts
webserver.log
ngrok.log
lhr.log
serve_public.out
*.out
# the deploy ships the compact ev_table.npz; the big pkl is a local artifact
ev_table.pkl
